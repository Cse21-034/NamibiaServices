import React from "react";

// This component is hidden for business listings as we don't support reservations
// Business listings are for directory purposes only
const MobileFooterSticky = () => {
  return null;
};

export default MobileFooterSticky;
